{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "pkgs": ["python310"]
    }
  },
  "deploy": {
    "startCommand": "streamlit run streamlitapp.py --server.port=$PORT --server.address=0.0.0.0"
  }
}
